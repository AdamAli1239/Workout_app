const express = require("express");

const router = express.Router();

router.get("/", {});

router.get("/:id", {});

router.post("/create", {});

router.patch("/:id", {});

router.delete("/:id", {});
